<h1><%= @user.name %></h1>

<h2>Organising</h2>
<%= render @user.events %>

<h2>Attending</h2>
<%= render @user.attended_events.where("date > ?", Time.now) %>

<h2>Attended</h2>
<%= render @user.attended_events.where("date < ?", Time.now) %>
